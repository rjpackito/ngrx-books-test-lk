{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "module": "es2015",
    "types": [
      "node"
   ],
   "typeRoots": [
      // add path to @types
      "node_modules/@types"
   ]
  },
  "exclude": [
    "src/test.ts",
    "**/*.spec.ts"
  ]
}
